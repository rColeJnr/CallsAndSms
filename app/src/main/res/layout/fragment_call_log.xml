<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/callDirection"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_incomining"
        android:contentDescription="@string/call_direction"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_toEndOf="@id/callDirection"
        android:layout_toStartOf="@id/callBack"
        android:layout_marginHorizontal="8dp"
        android:layout_centerVertical="true">

        <TextView
            android:id="@+id/number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:maxLines="1"
            android:textColor="@color/material_on_surface_emphasis_high_type"
            android:textSize="16sp"/>

        <TextView
            android:id="@+id/date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:maxLines="1"
            android:textColor="@color/material_on_surface_emphasis_medium"
            android:textSize="14sp"/>

    </LinearLayout>

    <ImageButton
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:id="@+id/callBack"
        android:src="@drawable/ic_phone_"
        android:contentDescription="@string/call_this_number"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        style="@style/Widget.AppCompat.ActionButton.Overflow"/>

</RelativeLayout>